name: docker-cd

on:
  push:
    branches:
      - main
    workflow_dispatch:

jobs:
  build-and-push-ui:
    name: Build and Push UI Docker Image
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Extract version from package.json
        run: echo "VERSION=$(jq -r .version platform/ui/package.json)" >> $GITHUB_ENV

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./platform/ui/Dockerfile
          push: true
          tags: |
            teawithsand/daia-ui:${{ env.VERSION }}
            teawithsand/daia-ui:latest

  build-and-push-api:
    name: Build and Push Blockchain API Docker Image
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Extract version from package.json
        run: echo "VERSION=$(jq -r .version platform/blockchain-api/package.json)" >> $GITHUB_ENV

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./platform/blockchain-api/Dockerfile
          push: true
          tags: |
            teawithsand/daia-blockchain-api:${{ env.VERSION }}
            teawithsand/daia-blockchain-api:latest

  build-and-push-demo-frontend:
    name: Build and Push Demo Frontend Docker Image
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Extract version from package.json
        run: echo "VERSION=$(jq -r .version demo/agents-demo-ui/package.json)" >> $GITHUB_ENV

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./demo/agents-demo-ui/Dockerfile
          push: true
          tags: |
            teawithsand/daia-demo-frontend:${{ env.VERSION }}
            teawithsand/daia-demo-frontend:latest

  build-and-push-demo-backend:
    name: Build and Push Demo Backend Docker Image
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Extract version from package.json
        run: echo "VERSION=$(jq -r .version demo/agents-demos-backend/package.json)" >> $GITHUB_ENV

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./demo/agents-demos-backend/Dockerfile
          push: true
          tags: |
            teawithsand/daia-demo-backend:${{ env.VERSION }}
            teawithsand/daia-demo-backend:latest

  build-and-push-docs:
    name: Build and Push Docs Docker Image
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Extract version from package.json
        run: echo "VERSION=$(jq -r .version docs/package.json)" >> $GITHUB_ENV

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./docs/Dockerfile
          push: true
          tags: |
            teawithsand/daia-docs:${{ env.VERSION }}
            teawithsand/daia-docs:latest