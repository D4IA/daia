# PNPM workspace

This package is not a part of PNPM workspace, since otherwise PNPM files to recognize local .npmrc, which is required for hugo to run properly.

Either run `npm install` or `pnpm install --ignore-workspace` to install node_modules in this package.