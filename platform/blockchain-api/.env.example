# Blockchain Bridge Configuration

# Requests per second limit for WhatsOnChain API
# Default: 3 (public API limit)
# Set to 20 if you have an API key
BSV_RPS=3

# API Key for WhatsOnChain (optional)
# Leave empty to use public API (limited to 3 RPS)
BSV_API_KEY=
