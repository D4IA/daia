/**
 * Address type representing a BSV address as a string.
 *
 * Which network it is depends on the context where it is used.
 *
 * To get it use PublicKey.toAddress(network) method from @daia/blockchain package.
 */
export type BlockchainAddress = string;
